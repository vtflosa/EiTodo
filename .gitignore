# --- Python ---
__pycache__/
*.py[cod]

# Virtual environments (if you create one later)
.venv/
venv/
env/

# --- Editors / IDE ---
.idea/
*.swp

# --- Generated config (created on first launch, machine-specific) ---
config.INI
param.lock

# --- User data (regenerated by the app; never commit personal todo data) ---
param.json
logs/
save/

# --- Test / stray artifacts ---
/new location/

# Note: source/ (icon.psd) is intentionally NOT ignored — it is a design
# source asset you may want to version. Run `git add source/` to track it.
